NEXT_PUBLIC_SUPABASE_URL=<key>
NEXT_PUBLIC_SUPABASE_ANON_KEY=<key>
SUPABASE_SERVICE_ROLE_KEY=<key>

GEMINI_API_KEY=<key>

STRIPE_SECRET_KEY=your_stripe_secret_key
STRIPE_PUBLISHABLE_KEY=your_stripe_publishable_key

GOOGLE_CLIENT_ID=your-client-id
GOOGLE_CLIENT_SECRET=your-client-secret

# Canonical app URL for OAuth redirect_uri (avoids redirect_uri_mismatch). Must match the origin you open the app with.
# Add exactly https://yourdomain.com/api/youtube/connect/callback (or http://localhost:3000/api/youtube/connect/callback for local) to GCP → Credentials → OAuth client → Authorized redirect URIs.
NEXT_PUBLIC_APP_URL=http://localhost:3000

# YouTube Data API v3 key for public channel/video lookup (Import by URL in Studio)
YOUTUBE_DATA_API_KEY=your-youtube-data-api-key

NEXT_PUBLIC_SITE_URL=http://localhost:3000

